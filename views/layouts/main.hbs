<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Blog</title>
    <link rel="stylesheet" href="/css/style.css">

</head>

<body>
    <header class="header">Tech Blog
        <nav>
            <a href="/" class="{{#if isHome}}hidden{{/if}}">Home</a>
            {{#if email}}
            <a href="/logout">Log Out</a>
            {{else}}
            {{#if isLoggedIn}}
            <a href="/dash">Dashboard</a>
            {{else}}
            <a href="/login" class="{{#if isLogin}}hidden{{/if}}">Login</a>
            {{/if}}
            {{/if}}
            <p class="{{#if isHome}}hidden{{/if}}{{#if isLogin}} hidden{{/if}}{{#if isRegister}} hidden{{/if}}">Welcome,
                {{email}}</p>

        </nav>
    </header>
    {{{body}}}
</body>

</html>