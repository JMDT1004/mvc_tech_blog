<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Blog</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/style.css">

</head>

<body>
    <header class="header">Tech Blog
        <nav>
            <a href="/" class="{{#if isHome}}{{/if}}">Home</a>
            {{#if username}}
            <a href="/logout">Log Out</a>
            {{else}}
            {{#if isLoggedIn}}
            <a href="/dash" class="{{#if isHome}}hidden{{/if}}">Dashboard</a>
            {{else}}
            <a href="/login" class="{{#if isLogin}}{{/if}}">Login</a>
            {{/if}}
            <a href="/Register" class="{{#if isLogin}}{{/if}}">Register</a>
            {{/if}}
            <p class="{{#if isHome}}{{/if}}{{#if isLogin}} {{/if}}{{#if isRegister}} {{/if}}">Welcome,
                {{username}}</p>
        </nav>
    </header>
    {{{body}}}
</body>

</html>